name: axora
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.11.4

dependencies:
  flutter:
    sdk: flutter
  flutter_downloader: ^1.12.0
  flutter_gemma: ^1.0.0-rc.1
  flutter_gemma_litertlm: ^1.0.0-rc.1
  flutter_markdown_plus: ^1.0.12
  path_provider: ^2.1.6
  permission_handler: ^12.0.3
  http: ^1.2.0
  record: ^5.2.1
  audioplayers: ^6.0.0
  image_picker: ^1.2.3
  file_picker: ^8.1.3
  syncfusion_flutter_pdf: ^26.2.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

dependency_overrides:
  record_linux: 1.3.1

flutter:
  uses-material-design: true
